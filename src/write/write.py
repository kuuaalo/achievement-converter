# aja testi tiedosto, xml python kirjasto? python listan xmlksi

import time

class Write:

    def __init__(self, file_name, format, tero):
      
        if file_name: 
            self.file_name = file_name
            pass
        else:
            print: ("wrong file name")   
            return False 

        if format:
            self.format = format
        else:
            print: ("wrong format")
            return False

        
    def run(self)
        pass


